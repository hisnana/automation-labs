# automation-labs
Demos honestas para transición a QA/Automation (Python).

- `rest_etl/`: CLI que normaliza un CSV → CSV (ETL simple).
- `playwright_demo/`: test mínimo con Playwright (Pytest).
- `logging_kata/`: ejemplo de logs + pruebas unitarias.
- CI: GitHub Actions básico ejecutando `pytest`.

> Estado: aprendizaje activo. Abierto a feedback.
